package utils

const (
	RATE_LIMIT_EXCEEDED   = "rate limit exceeded"
	INVALID_URL           = "invalid URL"
	INVALID_URL_LOCALHOST = "invalid URL: localhost"
	INVALID_URL_TOO_SHORT = "invalid URL: too short"
	SHORT_EXISTS          = "url short is already exist"
	INTERNAL_ERROR        = "internal error"
	UNREADABLE_BODY       = "unreadable request body"
	INVALID_JSON          = "invalid JSON"
	REQUIRED_FIELD        = "required field"
	FORM_EXPECTED         = "form expected"
	FAILED_PARSING_FORM   = "failed parsing form"
)
